import { Text } from "react-native";

const FormTitle = ({ text }) => {
    <Text style={{ fontFamily: 'Roboto-Medium', fontSize: 30, letterSpacing: 0.3, textAlign: 'center' } }>{text}</Text>
}

export default FormTitle;